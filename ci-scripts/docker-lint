#!/usr/bin/env bash
set -euo pipefail
# Run Ruff linter inside Docker container.
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$SCRIPT_DIR"

if ! docker info >/dev/null 2>&1; then
  echo "Docker not available"
  exit 0
fi

exec docker compose run --rm lint