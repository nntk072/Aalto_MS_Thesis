#!/usr/bin/env bash
set -euo pipefail
# Create a fresh .venv and install the package.
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$SCRIPT_DIR"

if [ -d .venv ]; then
  rm -rf .venv
fi

uv venv --python 3.12
uv sync --extra dev
uv pip install -e .